import * as React from "react";
import { View,Text  } from "react-native";
import tw from "twrnc";

export default function ProductosScreen() {
    return (
      <View style={tw`bg-violet-400`}>
        
      </View>
    );
  }